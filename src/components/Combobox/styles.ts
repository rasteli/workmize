export const styles = {
  comboboxMenuStyle: (
    color: string,
    backgroundColor: string
  ): React.CSSProperties => ({
    color,
    backgroundColor,

    padding: 10,
    borderWidth: 2,
    borderRadius: 15,

    borderColor: "#CBD5E0",
    listStyleType: "none"
  })
}
