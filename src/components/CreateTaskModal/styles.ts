export const styles = {
  inputBlock: (gap: number): React.CSSProperties => ({
    marginBottom: gap,
    position: "relative"
  }),

  form: {
    display: "flex",
    flexDirection: "column"
  } as React.CSSProperties
}
