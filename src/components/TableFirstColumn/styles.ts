export const styles = {
  container: {
    display: "flex",
    alignItems: "center"
  } as React.CSSProperties,

  searchInput: {
    height: 30,

    borderWidth: 2,
    color: "#A0AEC0",
    position: "absolute"
  } as React.CSSProperties,

  searchButton: {
    position: "absolute"
  } as React.CSSProperties
}
